import { Tooltip } from "@chakra-ui/react";

const test = () => {
  return (
    <div>
      <Tooltip label="Hey, I'm here!" aria-label="A tooltip">
        Hover me
      </Tooltip>
    </div>
  );
};

export default test;
